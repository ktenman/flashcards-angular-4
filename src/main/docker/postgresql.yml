version: '2'
services:
    flashcards-postgresql:
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/flashcards/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=flashcards
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
